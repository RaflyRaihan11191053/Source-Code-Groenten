import mysql.connector

db = mysql.connector.connect(
  host="localhost",
  user="root",
  passwd="",
  database = "veggie_for_you"
)

cursor = db.cursor()
cursor.execute("CREATE DATABASE veggie_for_you")

print("Database berhasil dibuat!")
